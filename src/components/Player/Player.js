import React from 'react';

import { MenuContainer } from "../";

import './Player.css';

// const videoList = [];

export const Player = () => {
    return (
        <>
        <MenuContainer />
        Player
        </>
    );
};