import React from 'react';

import { MenuContainer } from "../";

import './Player.css';

export const Player = () => {
    return (
        <>
        <MenuContainer />
        Player
        </>
    );
};